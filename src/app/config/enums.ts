export enum Sender { Bot = 'bot', Human = 'human'}
export enum Status { Correct = 'correct', Incorrect = 'incorrect'}
export enum UserType { Me = 'me', Other = 'other', System = 'system' }
export enum PartnerType {Human = 'human', Bot = 'bot', Unkonwn = 'unknown'};

export enum DisplayText  {
  TryAgain = 'Try Again',
  SpotOn = 'Spot On!',
  Wrong = 'Wrong',
}
